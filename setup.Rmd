```{r ggplot-theme,include=FALSE,purl=FALSE,cache=FALSE}
library(ggplot2)
theme_set(theme_bw())

```{r setup-child,include=FALSE,purl=FALSE,cache=FALSE}
library(knitr)
opts_chunk$set(
  cache=TRUE,
  cache.path="tmp/cache/",
  eval=TRUE,
  include=TRUE,
  echo=TRUE,
  purl=TRUE,
  dev='png',
  dev.args=list(bg='transparent'),
  dpi=300,
  error=FALSE,
  fig.pos="h!",
  fig.align='center',
  fig.height=4,fig.width=6.83,
  fig.lp="fig:",
  fig.path="tmp/figure/",
  fig.show='asis',
  highlight=TRUE,
  message=FALSE,
  progress=TRUE,
  prompt=FALSE,
  results="markup",
  size='small',
  strip.white=TRUE,
  tidy=FALSE,
  warning=FALSE,
  comment=NA
)

options(
  width=60, # number of characters in R output before wrapping
  keep.source=TRUE,
  encoding="UTF-8"
)
```
